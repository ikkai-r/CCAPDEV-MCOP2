{{#if parent_comment_id}}
        <div class="all-comment">
        <div class="row comment" id="comment" style="margin-left: 20px"> 

            <!-- Votes -->
            <div class="col-1 com-votes-cont" style="display: flex; flex-direction: column; justify-content: center; align-items: center;">
                <input type="hidden" value="{{_id}}" name="this_comment_id" id="this_comment_id"> 
                    {{#if up}}
                    <div><i class="comment_id fa-solid fa-circle-up comment-proper-votes" style="color: #292F33; font-size: x-large"></i></div>
                    {{else}}
                     <div><i class="comment_id fa-regular fa-circle-up comment-proper-votes" style="color: #292F33; font-size: x-large"></i></div>
                    {{/if}}
                <div class="comment-vote-cont"><span class="com-prop-amnt">0</span></div>
                 {{#if down}}
                        <div><i class="comment_id fa-solid fa-circle-down comment-proper-votes" style="color: #292F33; font-size: x-large"></i></div>
                  {{else}}
                         <div><i class="comment_id fa-regular fa-circle-down comment-proper-votes" style="color: #292F33; font-size: x-large"></i></div>
                  {{/if}}
            </div>
            <div class="col-11">
                <!-- Username and Date Commented -->
                <div class="row">
                    <div class="col-1">
                        <img src="{{username.profile_pic}}" class="comment-profile-pic">
                    </div>
                    <div class="col-11">
                        <div class="row">
                            <div class="col-11">
                                                        <div class="comment-username"><a id="username_comment" href="/user/{{username.username}}">{{username.username}}</a></div>
                        <div class="comment-username">{{comment_date}}</div>
                        
                            </div>
                            <div class="col-1">
                                 {{#if is_edited}}
                            <!-- Indicate if post is edited -->
                            <p class="edited-tag-post" id="edited-tag-post" style="margin-left: -40px;">(edited)</p>
                            {{/if}}
                        <div class="comment-extras">
                    <!-- <span class="edited" title="This post has been edited.">edited</span> -->
                    <button class="comment-options dropdown" id="comment-options-dropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fa-solid fa-ellipsis fa-l p-2"></i>
                    </button>
                    <div class="comment-options-menu dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                        <a class="dropdown-item" href="/post/editc-{{_id}}" id="edit-comment">Edit comment</a>
                        <input type="hidden" value="{{_id}}" id="comment_id">
                        <button class="dropdown-item delete-comment-btn" id="delete-comment-btn">Delete comment</button>
                    </div>
                </div>
                            </div>
                        </div>


             
                    </div>
                </div>
                
                <!-- Comment contents -->
                <div class="comment-content"> {{comment_content}}               </div>
                <span class="comment-reply" onclick="onClickRep(event)">reply</span>
            </div>
                     {{#each replies}}
                    {{> comments }}
                {{/each}}
        </div>
    </div>
    {{else}}

    <!-- Comment portion -->
    <div class="all-comment">
        <div class="row comment" id="comment">

            <!-- Votes -->
            <div class="col-1 com-votes-cont" style="display: flex; flex-direction: column; justify-content: center; align-items: center;">
                 <input type="hidden" value="{{_id}}" name="this_comment_id" id="this_comment_id"> 
                    {{#if up}}
                         <div><i class="comment_id fa-solid fa-circle-up comment-proper-votes" style="color: #292F33; font-size: x-large"></i></div>
                    {{else}}
                        <div><i class="comment_id fa-regular fa-circle-up comment-proper-votes" style="color: #292F33; font-size: x-large"></i></div>
                    {{/if}}
                <div class="comment-vote-cont"><span class="com-prop-amnt">{{total}}</span></div>
                  {{#if down}}
                        <div><i class="comment_id fa-solid fa-circle-down comment-proper-votes" style="color: #292F33; font-size: x-large"></i></div>
                  {{else}}
                         <div><i class="comment_id fa-regular fa-circle-down comment-proper-votes" style="color: #292F33; font-size: x-large"></i></div>
                  {{/if}}
               
            </div>
            <div class="col-11">
                <!-- Username and Date Commented -->
                <div class="row">
                    <div class="col-1">
                        <img src="{{username.profile_pic}}" class="comment-profile-pic">
                    </div>
                    <div class="col-11">
                        <div class="row">
                            <div class="col-11">
                                                        <div class="comment-username"><a id="username_comment" href="/user/{{username.username}}">{{username.username}}</a></div>
                        <div class="comment-username">{{comment_date}}</div>
                        
                            </div>
                            <div class="col-1">
                                 {{#if is_edited}}
                            <!-- Indicate if post is edited -->
                            <p class="edited-tag-post" id="edited-tag-post" style="margin-left: -40px;">(edited)</p>
                            {{/if}}
                        <div class="comment-extras">
                    <!-- <span class="edited" title="This post has been edited.">edited</span> -->
                    <button class="comment-options dropdown" id="comment-options-dropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <i class="fa-solid fa-ellipsis fa-l p-2"></i>
                    </button>
                    <div class="comment-options-menu dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                        <a class="dropdown-item" href="/post/editc-{{_id}}" id="edit-comment">Edit comment</a>
                        <input type="hidden" value="{{_id}}" id="comment_id">
                        <button class="dropdown-item delete-comment-btn" id="delete-comment-btn">Delete comment</button>
                    </div>
                </div>
                            </div>
                        </div>


             
                    </div>
                </div>
                
                <!-- Comment contents -->
                <div class="comment-content"> {{comment_content}} </div>
                <span class="comment-reply" onclick="onClickRep(event)">reply</span>
            </div>
                {{#each replies}}
                    {{> comments }}
                {{/each}}
        </div>
    </div>

    {{/if}}